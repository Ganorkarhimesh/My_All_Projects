# Supabase project credentials (used ONLY for optional auth + usage-count
# logging — no file content is ever transmitted or stored).
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Site URL, used for the OAuth redirect
NEXT_PUBLIC_SITE_URL=http://localhost:3000
